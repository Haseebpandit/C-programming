#include <stdio.h>
#include <stdlib.h>

int main()
{

      char a[100], c[100];
      gets(a);
      int i;
      while (a[i]!='\0')
      {
          c[i]=a[i];
          i++;
      }
      puts(c);


    return 0;
}
